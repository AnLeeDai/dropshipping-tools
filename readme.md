# Bộ công cụ Dropshipping

Biên dịch:

```powershell
dotnet build .\DropshippingTools.Native\DropshippingTools.Native.csproj
```

Đóng gói bộ cài setup và feed cập nhật Velopack:

```powershell
.\scripts\build-velopack-release.ps1
```
